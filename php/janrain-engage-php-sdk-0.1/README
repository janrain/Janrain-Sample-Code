This SDK consists of the engage.lib.php (the SDK),
 the test console to try it out, and some samples.

SDK:
Currently only has auth_info and get_contacts implemented.
 engage.lib.php

Example Token URL PHP:
A small example token URL script that uses the library.
 engage-token-url.php
  Uses:
    engage.lib.php
    engage-contacts.php
    engage-conf.php

Test Console:
Just place the SDK folder in a php enabled location 
then load the console folder location in your browser.
 index.php
 index.inc.php
 style.css
 console.conf.php


Requirements:
 PHP version >= 5
  Required PHP addons:
 	 cURL with SSL
 	 JSON
 	 simpleXML
