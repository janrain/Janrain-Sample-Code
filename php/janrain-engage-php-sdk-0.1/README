This SDK consists of the engage.lib.php (the SDK) 
as well as the test console to try it out.

SDK:
 engage.lib.php

Test Console:
 index.php
 index.inc.php
 style.css

Just place the folder in a php enabled location then
load the console folder location in your browser.

Requirements:
 PHP version >= 5
  Required PHP addons:
 	 cURL with SSL
 	 JSON
 	 simpleXML
